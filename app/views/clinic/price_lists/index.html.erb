<%= render "shared/patient_records/provide_patient_record_side_bar", patient_record: @patient_record, 
  scope: :clinic, current_site: :price_lists %>
<section class="content-header">
  <h1><%= t ".price_lists_activation" %></h1>
</section>
<section class="content">
  <div class="box">
    <div class="box-body">
      <table class="table table-hover">
        <thead>
          <th><%= PatientRecord.human_attribute_name :id %></th>
          <th><%= PatientRecord.human_attribute_name :item %></th>
          <th><%= PatientRecord.human_attribute_name :price %></th>
        </thead>
        <tbody id="price-lists">
          <%= render partial: :price_list, collection: @price_lists, as: :price_list %>
        </tbody>
      </table>
    </div>
  </div>
</section>
